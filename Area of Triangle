#include<math.h>
#include<bits/stdc++.h>
using namespace std;
int main()
{
    float a,b,c;
    cin>>a>>b>>c;
    float s=(a+b+c)/2;
    float area=sqrt(s*((s-a)*((s-b)*(s-c))));
    cout<<fixed<<setprecision(2)<<area;
}
